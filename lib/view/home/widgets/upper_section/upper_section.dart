import 'package:flutter/material.dart';

class UpperSectionInHomePage extends StatelessWidget {
  const UpperSectionInHomePage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}

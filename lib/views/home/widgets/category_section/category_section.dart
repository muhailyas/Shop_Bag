import 'package:flutter/material.dart';

class CategorySecotionInHomePage extends StatelessWidget {
  const CategorySecotionInHomePage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
